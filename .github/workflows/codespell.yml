name: codespell
on: [push, pull_request]
jobs:
  codespell:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v5
    - uses: codespell-project/actions-codespell@v2
      with:
        path: src
        ignore_words_list: aci,curev,datas,indx,olt,ot,pevent,te
